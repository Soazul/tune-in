// import React, { useState, useEffect } from 'react';
// import { toast, ToastContainer } from 'react-toastify';
// import 'react-toastify/dist/ReactToastify.css';

export default function Timer() {
    // const [seconds, setSeconds] = useState(10); // Set the initial countdown to 10 seconds
  
    // useEffect(() => {
    //   if (seconds > 0) {
    //     const timerId = setTimeout(() => setSeconds(seconds - 1), 1000);
    //     return () => clearTimeout(timerId); // Cleanup timer on component unmount
    //   } else {
    //     // Show the toast notification when the timer reaches zero
    //     toast("Time's up!", {
    //       position: 'top-right',
    //       autoClose: 3000,
    //       hideProgressBar: false,
    //       closeOnClick: true,
    //       pauseOnHover: true,
    //       draggable: true,
    //       progress: undefined,
    //     });
    //   }
    // }, [seconds]);
  
    // return (
    //   <div>
    //     <h1>{seconds > 0 ? `Time Remaining: ${seconds}s` : "Time's up!"}</h1>
    //     <ToastContainer /> {/* Container for toast notifications */}
    //   </div>
    // );
  }